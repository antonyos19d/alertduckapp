import 'package:flutter/material.dart';

class DispositivosPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('DISPOSITVOS')),
    );
  }
}
